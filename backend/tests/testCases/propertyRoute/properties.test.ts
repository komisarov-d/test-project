/* import supertest from 'supertest';
import app from '../../../src/app'; */

/* import { ResponseStatusCode } from '../../../src/common/enums';
import { getApiV1Route } from '../../helpers';
import { AuthRoute } from '../../common/enums';

const request = supertest(app); */

describe('GET /properties Endpoint', () => {
	// eslint-disable-next-line no-tabs
	// const testEndpoint = getApiV1Route(AuthRoute.Properties);

	test('should return all properties', async () => {
		/* const response = await request.get(testEndpoint).send();

		expect(response.status).toEqual(ResponseStatusCode.Ok);
		expect(response.body).toMatch(); */
	});
});
